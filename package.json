{
  "name": "unimatrix",
  "version": "0.1.0",
  "description": "Universal AI memory persistence and cross-device continuity layer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "clean": "turbo run clean && rm -rf node_modules .turbo dist",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:prod": "docker-compose -f docker-compose.prod.yml up -d",
    "type-check": "tsc --noEmit",
    "mobile:eas:login": "cd apps/mobile && PATH=$(pwd)/node_modules/.bin:$(pwd)/../node_modules/.bin:$PATH eas login",
    "mobile:eas:init": "cd apps/mobile && PATH=$(pwd)/node_modules/.bin:$(pwd)/../node_modules/.bin:$PATH eas init --id a93ce64a-bd02-4777-a432-35f72349253d",
    "mobile:build": "cd apps/mobile && PATH=$(pwd)/node_modules/.bin:$(pwd)/../node_modules/.bin:$PATH EAS_BUILD_NO_EXPO_GO_WARNING=true EXPO_NO_TELEMETRY=1 eas build --profile production --non-interactive --no-wait",
    "mobile:build:all": "cd apps/mobile && PATH=$(pwd)/node_modules/.bin:$(pwd)/../node_modules/.bin:$PATH EAS_BUILD_NO_EXPO_GO_WARNING=true EXPO_NO_TELEMETRY=1 eas build --platform all --profile production --non-interactive --no-wait",
    "mobile:build:ios": "cd apps/mobile && PATH=$(pwd)/node_modules/.bin:$(pwd)/../node_modules/.bin:$PATH EAS_BUILD_NO_EXPO_GO_WARNING=true EXPO_NO_TELEMETRY=1 eas build --platform ios --profile preview --non-interactive --no-wait",
    "mobile:build:android": "cd apps/mobile && PATH=$(pwd)/node_modules/.bin:$(pwd)/../node_modules/.bin:$PATH EAS_BUILD_NO_EXPO_GO_WARNING=true EXPO_NO_TELEMETRY=1 eas build --platform android --profile production --non-interactive --no-wait",
    "mobile:build:preview": "cd apps/mobile && PATH=$(pwd)/node_modules/.bin:$(pwd)/../node_modules/.bin:$PATH EAS_BUILD_NO_EXPO_GO_WARNING=true EXPO_NO_TELEMETRY=1 eas build --profile preview --non-interactive --no-wait"
  },
  "_eas_mobile_note": "Use these for the React Native / Expo app (apps/mobile). They guarantee the correct working directory. See apps/mobile/README.md and ACCOUNTS_AND_SECRETS.md for details. Direct npx eas-cli from here is the #1 cause of 'expo-modules-autolinking' errors and huge uploads. NOTE: iOS production requires $99/yr Apple Developer membership (currently unaffordable for user) - :ios alias uses preview/simulator (free). Android full prod ok.",
  "dependencies": {
    "next": "16.2.9"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^9",
    "eslint-config-next": "16.2.9",
    "prettier": "^3.8.4",
    "turbo": "^2.9.18",
    "typescript": "^5.9.2"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "overrides": {
      "postcss": ">=8.5.10",
      "uuid": ">=11.1.1",
      "esbuild": ">=0.28.1"
    },
    "packageExtensions": {
      "xcode@3.0.1": {
        "dependencies": {
          "uuid": "^11.1.1"
        }
      }
    }
  }
}
