import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Sign In - Mempalace',
  description: 'Sign in to your memory palace',
};

export default function LoginPage() {
  return <LoginForm />;
}
